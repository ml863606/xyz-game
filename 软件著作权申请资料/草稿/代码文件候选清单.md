# 代码文件候选清单

请先确认要抽取哪些源码文件，再运行代码材料抽取。

本清单只列出候选源码证据，不默认决定抽取文件。
模型需要先理解项目业务、页面入口和源码职责，再填写 `selected/model_reason`。
当前已选约 24 页，全部候选源码约 24 页。

```text
STOP_FOR_USER
NEXT_ACTION: 请由模型先填写 草稿/代码文件选择.json 的抽取选择和选择理由，再让用户确认；确认后运行 confirm_stage.py --stage code-selection。
```

确认方式：

1. 模型根据项目业务和代码入口选择最能体现软件功能的文件。
2. 把需要抽取的文件设为 `selected: true`，并填写 `model_reason`。
3. 代码材料按完整文件抽取并去除纯空行，不支持只抽取某个文件的中间行段。
4. 用户确认模型选择后，再记录 `code-selection` 门禁。

## 默认选中文件

| 文件 | 行数 | 模型选择理由 |
| --- | ---: | --- |
| `scripts/balloon.gd` | 152 | 气球目标核心模块，定义普通、金色、炸弹、护盾、时钟、雷电、黑洞、火箭和彩虹爆破等目标类型，体现气球绘制、移动、碰撞和击破状态。 |
| `scripts/dart.gd` | 56 | 飞镖核心模块，体现发射初始化、重力和风力影响、飞行轨迹、拖尾绘制、碰撞形状和出界销毁逻辑。 |
| `scripts/effect_ring.gd` | 30 | 冲击波特效模块，用于彩虹爆破、黑洞等视觉反馈，体现圆环扩散、透明度衰减和生命周期管理。 |
| `scripts/game.gd` | 861 | 主游戏控制模块，覆盖场景构建、HUD、输入拖拽、飞镖发射、气球生成、碰撞结算、得分连击、任务、能量狂热、关卡胜负和最高分调用，是最能体现软件功能的核心源码。 |
| `scripts/level_config.gd` | 32 | 关卡配置模块，记录关卡编号、目标分数、生成间隔、移动速度、气球概率和风力强度，体现关卡递进规则。 |
| `scripts/lightning_bolt.gd` | 38 | 雷电视觉效果模块，体现特殊气球触发后的闪电链显示和短生命周期特效。 |
| `scripts/platform_adapter.gd` | 32 | 平台适配模块，体现本地最高分读取保存和未来抖音运行环境/分享能力隔离边界。 |
| `scripts/rocket_projectile.gd` | 44 | 火箭追踪模块，体现火箭气球触发后的目标追踪、飞行轨迹、命中检测和反馈回调。 |
| `test/capture_frames.gd` | 29 | 截图验证辅助脚本，用于自动演示和帧捕获，作为候选源码不足 60 页时的真实项目补充材料。 |
| `test/presentation.gd` | 7 | 演示入口辅助脚本，用于触发自动演示流程，作为项目真实测试/展示代码补充。 |
| `scenes/main.tscn` | 7 | 主入口场景文件，绑定主游戏脚本并体现软件启动场景结构，是运行入口配置材料。 |
| `test/auto_main.tscn` | 9 | 自动演示测试场景文件，绑定演示脚本，作为候选源码不足 60 页时的真实场景配置补充。 |

## 未选候选文件

| 文件 | 行数 | 证据类型 |
| --- | ---: | --- |
| 无 | 0 | 全部候选源码已选中 |
