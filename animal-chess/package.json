{
  "name": "animal-chess",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test tests/animalChessRules.test.ts tests/roomProtocol.test.ts",
    "build:web": "esbuild web-preview/src/main.ts --bundle --format=esm --target=es2020 --outfile=web-dist/main.js",
    "preview": "npm run build:web",
    "server": "tsx server/roomServer.ts"
  },
  "dependencies": {
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.3",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.25.5",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
