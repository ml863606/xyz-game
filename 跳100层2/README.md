# 跳100层2

Godot 4 竖向平台跳跃小游戏。

## 运行

```powershell
godot --path .
```

## 校验

```powershell
godot --headless --import --quit --path .
godot --headless --check-only --path .
```

## 操作

- `A/D` 或 `方向键`：左右移动
- 落到平台会自动起跳
- `R` 或 `Enter`：重新开始

目标是向上跳到第 100 层，收集宝石和弹簧，避开红色旋刃。
